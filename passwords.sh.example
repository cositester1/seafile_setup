#!/bin/bash

# This is an example file for your credentials.
# To use it, copy this file to 'passwords.sh' and fill in your actual passwords.
# The 'passwords.sh' file is ignored by git, so your credentials will not be committed.

# --- Credentials ---
# The root password for the MariaDB database.
export DB_ROOT_PASSWORD="changeme_db"

# The email address for the initial Seafile admin user.
export SEAFILE_ADMIN_EMAIL="admin@example.com"

# The password for the initial Seafile admin user.
export SEAFILE_ADMIN_PASSWORD="changeme_admin"
